server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5431/demo_connection
    username: postgres
    password: Mywhiteshirt29@

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:   # 👈 FIXED key (best practice format)
      defaultZone: http://localhost:9098/eureka/

  instance:
    prefer-ip-address: true